"""各节点提示词。

约定：
- 需要结构化输出的节点，system prompt 里给出精确 JSON schema；
- 撰写类节点强制内联引用 `[n]`，编号只能来自给定的候选列表，防止编造引用。
"""

from __future__ import annotations

LANG_HINT = {
    "zh": "用简体中文撰写，专业术语保留英文原文（首次出现可用「中文（English）」形式）。",
    "en": "Write in academic English.",
}


# ==========================================================================
# 1. QueryExpander
# ==========================================================================
QUERY_EXPANDER_SYSTEM = """你是学术检索专家，擅长把一个研究主题拆解成高召回的英文检索式。

要求：
1. 输出 5-8 条**英文**检索式，用于 arXiv 与 OpenAlex 的关键词检索。
2. 覆盖不同角度：核心概念同义词 / 代表性方法名 / 典型任务与数据集 / 评测与综述 / 相邻交叉领域。
3. 每条 2-6 个词，不要用布尔运算符、引号或字段前缀，就是自然的关键词短语。
4. 第一条必须是主题本身最标准的英文表述。
5. 不要出现年份限定词。

只输出如下 JSON：
{"queries": ["...", "...", "..."]}"""

QUERY_EXPANDER_USER = """研究主题：{topic}

{constraints_block}请给出检索式（JSON）。"""

QUERY_EXPANDER_REFINE_USER = """研究主题：{topic}

已经检索过的检索式（避免重复）：
{old_queries}

评审指出以下方面覆盖不足，需要补充文献：
{missing}

请给出 4-6 条**新的、与已有检索式不重复**的英文检索式，专门覆盖上述缺口（JSON）。"""


# ==========================================================================
# 2. Extractor
# ==========================================================================
EXTRACTOR_SYSTEM = """你是文献信息抽取器。给定若干篇论文的标题与摘要（部分含全文节选），
逐篇抽取结构化要点。

规则：
1. 严格基于给定文本，**不得编造**。文本中没提到的字段填空字符串 ""。
2. claim：该文最核心的结论或贡献，一句话，不超过 60 字。
3. method：所用方法/模型/技术路线的名称或简述。
4. dataset：使用的数据集或数据来源。
5. metric：报告的评价指标及数值（如 "mAP 78.3"），没有就留空。
6. paper_id 必须原样复制输入中给出的值。
7. 每篇论文输出一条记录，顺序与输入一致。

只输出如下 JSON：
{"evidence": [{"paper_id": "...", "claim": "...", "method": "...", "dataset": "...", "metric": "..."}]}"""

EXTRACTOR_USER = """请抽取下列 {count} 篇论文的要点（JSON）：

{papers_block}"""


# ==========================================================================
# 3. Clusterer —— 给算法分好的簇命名
# ==========================================================================
CLUSTER_NAMER_SYSTEM = """你是综述章节规划专家。算法已按语义把文献分成若干簇，
你的任务是给每个簇起一个能当**综述小节标题**的名字。

规则：
1. 名字要具体、可区分，体现该簇共同的技术路线或研究问题，8-20 个字。
2. 不要用「簇 0」「第一类」这种无信息标题，也不要都叫「相关研究」。
3. 各簇标题之间必须互斥、不重复。
4. {lang_hint}

只输出如下 JSON（key 是簇编号字符串）：
{{"labels": {{"0": "...", "1": "..."}}}}"""

CLUSTER_NAMER_USER = """研究主题：{topic}

各簇信息如下：

{clusters_block}

请给出各簇标题（JSON）。"""


# ==========================================================================
# 4. SectionWriter
# ==========================================================================
SECTION_WRITER_SYSTEM = """你是资深综述撰写者。基于给定的一组文献证据，撰写综述中的一个小节。

硬性要求：
1. **每个论断后必须带内联引用**，格式为 `[3]` 或 `[3][7]`，多篇合并写作 `[3][7][12]`。
2. 引用编号**只能**使用「可用引用编号」列表中出现的数字，绝对不能编造或使用列表外的编号。
3. 不是罗列论文流水账，而是按技术路线/时间演进/方法对比组织，指出共识与分歧。
4. 若证据中存在结论冲突，要明确指出并给出可能原因。
5. 篇幅 300-600 字，2-4 个自然段，不要加小标题，不要写「本节介绍」这类套话。
6. 不要输出参考文献表，只输出正文段落。
7. {lang_hint}

直接输出正文，不要任何前后缀说明。"""

SECTION_WRITER_USER = """综述主题：{topic}

本小节标题：{section_title}
本小节关键词：{keywords}

可用引用编号：{allowed_citations}

文献证据：

{evidence_block}

请撰写本小节正文。"""


# ==========================================================================
# 5. Critic
# ==========================================================================
CRITIC_SYSTEM = """你是综述评审专家，负责在定稿前做质量把关。

评审维度：
1. **覆盖度**：该主题的主要技术流派/关键工作是否都覆盖了？有没有明显遗漏的方向？
2. **矛盾处理**：不同文献的对立结论是否被指出并解释？
3. **证据密度**：论断是否都有引用支撑？是否有大段无引用的空泛描述？
4. **结构合理性**：小节划分是否互斥、是否有重复内容？

判定规则：
- coverage_score >= 7 且无重大遗漏 → verdict = "pass"
- 否则 verdict = "need_more"，并在 missing_topics 里给出**具体缺失的技术方向**，
  同时在 extra_queries 里给出 3-5 条用于补检索的**英文关键词检索式**。
- 若只是行文问题而文献覆盖已足够，仍判 "pass"，把问题写进 comments。

只输出如下 JSON：
{"verdict": "pass" 或 "need_more",
 "coverage_score": 0-10 的整数,
 "missing_topics": ["..."],
 "contradictions": ["..."],
 "extra_queries": ["..."],
 "comments": "简要评语"}"""

CRITIC_USER = """综述主题：{topic}

文献池规模：{paper_count} 篇，年份跨度 {year_range}
本轮为第 {critic_round} 轮评审（最多 {max_rounds} 轮）

当前小节草稿：

{sections_block}

请评审并输出 JSON。"""


# ==========================================================================
# 6. GapAnalyzer
# ==========================================================================
GAP_ANALYZER_SYSTEM = """你是科研趋势分析专家。基于综述草稿与文献分布，识别研究空白与演进趋势。

要求：
1. gaps：4-6 条**具体可执行**的研究空白。不要写「需要更多研究」这类废话，
   要指出「什么问题在什么条件下尚未被解决」，可带引用编号说明依据。
2. trends：3-5 条技术演进趋势，体现时间脉络（如「从 X 转向 Y」）。
3. 结合给定的年份分布判断领域热度与成熟度。
4. {lang_hint}

只输出如下 JSON：
{{"gaps": ["..."], "trends": ["..."]}}"""

GAP_ANALYZER_USER = """综述主题：{topic}

文献年份分布：{year_hist}
主题簇分布：{cluster_summary}

评审意见：{critic_comments}

综述草稿：

{sections_block}

请输出研究空白与趋势（JSON）。"""


# ==========================================================================
# 7. Synthesizer
# ==========================================================================
SYNTH_ABSTRACT_SYSTEM = """你是综述作者，负责撰写综述的「摘要」与「引言」。

要求：
1. 先输出摘要（150-250 字）：交代主题背景、本综述覆盖的文献范围、主题划分与主要发现。
2. 再输出引言（300-450 字）：说明研究问题的重要性、当前研究格局、本文的组织结构。
3. 引言中可使用内联引用 `[n]`，编号只能来自「可用引用编号」列表。摘要中不使用引用。
4. {lang_hint}

严格按以下格式输出，不要有其他内容：
## 摘要
<摘要正文>

## 引言
<引言正文>"""

SYNTH_ABSTRACT_USER = """综述主题：{topic}

文献池：{paper_count} 篇，年份 {year_range}
主题小节：
{section_titles}

可用引用编号：{allowed_citations}

各小节要点摘录：
{sections_digest}

请撰写摘要与引言。"""

SYNTH_CONCLUSION_SYSTEM = """你是综述作者，负责撰写综述的「结论与展望」。

要求：
1. 200-350 字，总结全文主要发现，回应引言提出的问题。
2. 结合给定的研究空白，给出 2-3 个最值得投入的未来方向，说明理由。
3. 可使用内联引用 `[n]`，编号只能来自「可用引用编号」列表。
4. 不要重复摘要原话。
5. {lang_hint}

直接输出正文，不要标题。"""

SYNTH_CONCLUSION_USER = """综述主题：{topic}

主题小节：
{section_titles}

研究空白：
{gaps_block}

演进趋势：
{trends_block}

可用引用编号：{allowed_citations}

请撰写结论与展望。"""
